<h1>場所編集</h1>

<%= form_with model: @place do |f| %>
  <div>
    <%= f.label :name, "場所名" %>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.submit "更新" %>
  </div>
<% end %>

<%= link_to "場所詳細へ", place_path(@place) %>